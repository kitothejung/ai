# Agent Handshake

- Claude and Codex should both treat the same repository files as the shared source of truth.
- Agent-local directories may exist, but they should not replace the shared workflow.
- If instructions conflict, prefer the top-level project files and shared memory.

